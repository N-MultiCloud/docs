# docs/source/_static — Sphinx Static Assets

Up: [`../CLAUDE.md`](../CLAUDE.md) · [`../../../CLAUDE.md`](../../../CLAUDE.md)

---

Static assets bundled into the rendered Sphinx site (CSS, favicon, custom
JS). Loaded by [`../conf.py`](../conf.py).

## Working Rules

- Read-only vendor source.
- Style overrides live here; the base theme comes from the Sphinx theme
  package declared in [`../../requirements.txt`](../../requirements.txt).
